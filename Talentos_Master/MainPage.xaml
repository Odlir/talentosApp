<UserControl x:Class="Talentos_Master.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:my="clr-namespace:Talentos_Master"          SizeChanged="UserControl_SizeChanged"
    mc:Ignorable="d" d:DesignWidth="Auto" d:DesignHeight="Auto" >
    <Grid x:Name="MainRoot" VerticalAlignment="Top" Background="#FFFFFFFF">
        <ScrollViewer x:Name="svUserControl" BorderThickness="0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,-9,0,0" VerticalScrollBarVisibility="Auto" >
            <Grid x:Name="Root" HorizontalAlignment="Stretch" >
                <Border x:Name="contenedor"  Width="Auto" Height="800"  Style="{StaticResource GlassBorderFondo}" BorderBrush="Transparent">
                    <Canvas Width="auto" Height="auto">
                        <Canvas.RenderTransform>
                            <ScaleTransform x:Name="CanvasScale" ScaleX="1" ScaleY="1"></ScaleTransform>
                        </Canvas.RenderTransform>
                        <Canvas x:Name="LayoutRoot">
                        </Canvas>
                    </Canvas>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
